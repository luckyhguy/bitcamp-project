[v0.9]
구현할 기능:

=> Linked List 클래스를 만든다.
=> Linked List 를 ClassRoomController에 적용한다.

산출물:
=> Student.java
=> EduApp.java (변경)
=> ClassRoomController.java (추가)
=> XxxController.java(변경)
=> Box.java(추가)
=> LinkedList.java (추가)
학습 목표:
=> LinkedList를 만들 수 있다.
=> LinkedList를 통해 
